# backend/requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# Used by Render for deployment (CPU only — no GPU on free tier).
# Local development uses uv with pyproject.toml for the CUDA build.
# ─────────────────────────────────────────────────────────────────────────────

# Supabase
supabase==2.10.0
python-dotenv==1.0.1

# Data fetching
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2

# Data wrangling
pandas==2.2.3
numpy==1.26.4

# ML
xgboost==2.1.1
scikit-learn==1.5.2

# PyTorch CPU build for Render.
# The --extra-index-url tells pip where to find the CPU-only wheel.
# This is the correct way to install CPU torch on a plain pip environment.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1

# API server
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2