# frontend/.env.local
# These are safe to expose in the browser — RLS restricts to read-only.
# Add these as Environment Variables in Vercel dashboard too.

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJh...
